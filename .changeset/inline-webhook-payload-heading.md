---
'speccy-renderer': patch
---

Place the webhook payload heading on the same row as its media type, keeping the heading at section weight and wrapping long media types instead of overflowing on narrow viewports.
