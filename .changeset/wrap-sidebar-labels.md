---
'speccy-renderer': minor
---

Add a `wrapSidebarLabels` option that wraps long sidebar endpoint labels onto a second line instead of truncating them on one, so summaries that share a long prefix stay distinguishable.
