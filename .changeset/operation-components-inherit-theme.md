---
'speccy-renderer': patch
---

Operation links, strips, cards, and previews now inherit the host page's theme by default, so they switch to dark mode alongside Docusaurus instead of staying light. Pass `theme` to pin a colour scheme.
